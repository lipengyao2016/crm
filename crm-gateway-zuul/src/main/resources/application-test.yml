server:
  port: 9090
  session:
    timeout: 1800
logging:
  level:
    tk.mybatis: TRACE
    root: info
    org.springframework.web: INFO
  file: \tmp\log\log.log
eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
